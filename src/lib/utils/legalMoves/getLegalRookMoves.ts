import type { FenNotationObjectType, PiecePositionInfoType } from "$lib/types/chess.types";

export const getLegalRookMoves = (gameObject: FenNotationObjectType, pieceInfo: PiecePositionInfoType) => {
    return [];
}