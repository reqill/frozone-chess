import type { FenNotationObjectType, PiecePositionInfoType } from "$lib/types/chess.types";

export const getLegalKnightMoves = (gameObject: FenNotationObjectType, pieceInfo: PiecePositionInfoType) => {
    return [];
}