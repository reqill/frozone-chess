import type { FenNotationObjectType, PiecePositionInfoType } from "$lib/types/chess.types";

export const getLegalQueenMoves = (gameObject: FenNotationObjectType, pieceInfo: PiecePositionInfoType) => {
    return [];
}