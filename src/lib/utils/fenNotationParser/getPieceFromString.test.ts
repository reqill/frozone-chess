import { expect, describe } from 'vitest';

describe('getPieceFromString', () => {
	/*const testCases = [
		{ input: 'P', expected: { piece: 'pawn', side: 'white' } },
		{ input: 'p', expected: { piece: 'pawn', side: 'black' } },
		{ input: 'R', expected: { piece: 'rook', side: 'white' } },
		{ input: 'r', expected: { piece: 'rook', side: 'black' } },
		{ input: 'N', expected: { piece: 'knight', side: 'white' } },
		{ input: 'n', expected: { piece: 'knight', side: 'black' } },
		{ input: 'B', expected: { piece: 'bishop', side: 'white' } },
		{ input: 'b', expected: { piece: 'bishop', side: 'black' } },
		{ input: 'Q', expected: { piece: 'queen', side: 'white' } },
		{ input: 'q', expected: { piece: 'queen', side: 'black' } },
		{ input: 'K', expected: { piece: 'king', side: 'white' } },
		{ input: 'k', expected: { piece: 'king', side: 'black' } },
	];

	it('should return the correct piece', () => {
		testCases.forEach(({ input, expected }) => {
			expect(getPieceFromString(input)).toEqual(expected);
		});
	});

	it('should throw an error if the piece is invalid', () => {
		expect(() => getPieceFromString('X')).toThrow();
	});*/

	// TODO: rewrite tests to match new store structure
	expect(true).toBe(true);
});
